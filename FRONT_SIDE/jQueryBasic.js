//selector $("div.class"); // 出现.class  最好和 标签共同使用$("div > .class") // 第一代后代selector$("input[name= elementAttribute]") //attribute//还可以使用简化的正则来使用属性选择器$("input[name |= ]")$("input[name ~= ]")$("input[name ^= ]")$("input[name $= ]")$("input[name != ]")$("input[name *= ]")//Pseudo-selectors $("a:first");$("tr:odd");$("#myForm :input") // select all input like element in a from $("div:enabled")//所有可用的元素 disabled=disabled  就不选择了$("div:disabled")//得到所有被禁用的元素$("div:visible")//elementOne.offsetWidth // offsetHeight  : 得到 除了 tr 元素外 所有dom元素 的实际宽度 : elem.nodeName/*得到实际的标签Name*/.toLowerCase() === "tr";$("div:hidden")// checked physical height and width  (except the tr : case when tr case (checked style is none ) end)$("div:gt(2)");// first 3 divs$("div:animated");// all currently animated divs $("div:checkbox");//$(":checked");//$(":contains('...')");//包含内容选择器$("");//5.用户查询 查询条样式修改好.重置按钮去掉.			var box = $.newWindow({				center : "",				title : '更新用户',				id : 'updateUser',				width :400 ,				height :450			});						box.find('.box-center').mask({				zIndex:box.css('zIndex'),				isWait:true			});						box.find('.box-center').html(json);			buttonView(box);			box.find('.box-center').unMask();		var $from = box.find("form1");			if($form.verifyForm()){			$form1.ajaxSubmit(options);		}						var $from = box.find("form1");							if(!$form.verifyForm()){							return;						}